<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="password in  rule-AD" type="Validation">
  <Description>This program will return all the objects from Identity Class</Description>
  <Source>
 

  
 import sailpoint.object.PasswordPolicy;
import sailpoint.api.PasswordGenerator;




     PasswordGenerator generator=new PasswordGenerator(context);
	 PasswordPolicy policy=context.getObject(PasswordPolicy.class,"Default PasswordPolicy");
  
if(policy!=null)
{
System.out.println("policy"+policy);
	 String newPassword=generator.generatePassword(policy);
System.out.println("newPassword"+newPassword);

	 System.out.println("new Password"+newPassword);
}
	 return newPassword;

 
  </Source>
</Rule>